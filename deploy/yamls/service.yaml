apiVersion: v1
kind: Service
metadata:
  name: kubeconfig-ui
  namespace: devops
spec:
  selector:
    app: kubeconfig-ui
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
